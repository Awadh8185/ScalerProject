[project]
name = "pii-redaction-tool"
version = "1.0.0"
description = "DOCX PII redaction service"
requires-python = ">=3.12"
dependencies = [
  "Flask==3.1.0",
  "python-docx==1.2.0"
]

[tool.vercel]
entrypoint = "api.redact:app"
